[package]
name = "scheduler"
version.workspace = true
edition.workspace = true

[dependencies]
common = { path = "../common" }
rand.workspace = true
thiserror.workspace = true

[dev-dependencies]
common = { path = "../common" }
